{% extends '@OHMediaBackend/form.html.twig' %}

{% block breadcrumbs %}
{{ bootstrap_breadcrumbs(
  bootstrap_breadcrumb('Files', 'file_index'),
  bootstrap_breadcrumb(file.id ? 'Edit' : 'Create'),
) }}
{% endblock %}
